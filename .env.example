# Get a key from https://aistudio.google.com/apikey
GOOGLE_GENERATIVE_AI_API_KEY=
# Optional, defaults to gemini-2.5-flash
GEMINI_MODEL=

# apps/mcp — optional, defaults to 0.0.0.0:8080 (Cloud Run sets PORT for you)
MCP_HOST=
MCP_PORT=
